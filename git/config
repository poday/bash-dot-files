[alias]
    lg = "log --color=always --all --graph --topo-order --pretty='%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative"
    pull = "pull --ff-only"

[color]
    ui = auto

[commit]
	verbose = true

[core]
	editor = code --wait
	autocrlf = false
	pager = less --no-init --quit-if-one-screen

[user]
	name = Patrick O'Day
	email = pcoday@gmail.com
[rerere]
	enabled = true
[fetch]
	writeCommitGraph = true
